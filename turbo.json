{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", "build/**", "apps/**/.next/**", "apps/**/.turbo/**"]
    },
    "dev": {
      "cache": false
    },
    "lint": {},
    "test": {},
    "prisma:generate": {
      "cache": false
    }
  }
}
